// NOUVEAU CONTENU (Syntaxe JS)
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Pour les projets Next.js 14/15, on s'assure que le mode 'transpileModules' est désactivé
  // et que le projet peut fonctionner normalement.
  // S'il n'y avait rien dans votre ancien fichier, copiez simplement ceci :
  // (Pas besoin d'options supplémentaires pour ce calendrier)
};

module.exports = nextConfig;